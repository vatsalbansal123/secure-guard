# SecureGuard backend dependencies.
# Curated to only what the app imports (see backend/*.py). The frontend has its
# own package.json. Regenerate intentionally — do NOT `pip freeze` a polluted env.

# --- Web / API ---
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4

# --- Config ---
python-dotenv==1.2.2

# --- LLM agent ---
langchain-core==1.4.7
langchain-openai==1.3.2
langgraph==1.2.5

# --- Database ---
SQLAlchemy==2.0.50
pyodbc==5.3.0

# --- Rate limiting ---
slowapi==0.1.9

# --- Testing (dev) ---
pytest==8.3.4
httpx==0.28.1

# --- Security pins (transitive deps, pinned to force the Snyk-flagged fixes) ---
starlette==1.3.1       # High: resource throttling + name resolution CVEs
cryptography==48.0.1   # High: out-of-bounds read
langsmith==0.8.18      # High: type confusion (pulled in by langchain-core)
